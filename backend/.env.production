# Add these environment variables to your Railway backend service

# CORS Origins - comma-separated list
CORS_ORIGINS=https://specfinder.io,https://www.specfinder.io,https://api.specfinder.io,https://constructionrag-frontend-production.up.railway.app,http://localhost:3000

# Backend API URL - used for webhook callbacks from Beam
BACKEND_API_URL=https://api.specfinder.io

# Ensure environment is set to production
ENVIRONMENT=production